## To do

- [ ] make carousel controls responsive
- [ ] make the carousel-items be responsible for their own width
- [ ] remove inline styles for HeroCarousel
- [x] try one with left/right arrows
- [x] clearly define the 'props' of this component (i.e width, colors palette, array of slides to display )
- [x] make not break when I wrap the whole thing in an `@layer components { }` 
- [x] support keyboard only navigation
- [x] do a carousel with multiple slides in view at a time

## Requirements

- [ ] can tab through carousel items
