---
title: Alert
description: A platform messaging element
---

import ShowComponent from '../../../components/doc-helpers/ShowComponent.astro';
import SuccessAlert from '../../../components/alerts/SuccessAlert.html';
import InfoAlert from '../../../components/alerts/InfoAlert.html';
import ErrorAlert from '../../../components/alerts/ErrorAlert.html';

### Success Alert

The colour scheme on this alert box indicates a success state message

<ShowComponent>
  <SuccessAlert />
</ShowComponent>

### Info Alert

The colour scheme on this alert box indicates an informational message

<ShowComponent>
  <InfoAlert />
</ShowComponent>

### Error Alert

The colour scheme on this alert box indicates a error state message

<ShowComponent>
  <ErrorAlert />
</ShowComponent>

### General Alert Variables

| Variable                 | Default Value           | Description                         |
| ------------------------ | ----------------------- | ----------------------------------- |
| `--alert-padding`        | `0.5em`                 | Padding around the content          |
| `--alert-font-weight`    | `var(--font-semi-bold)` | Weight of the font                  |
| `--alert-border-default` | `none`                  | Allow a border, deafult none        |
| `--alert-border-default` | `0`                     | Allow a border radius, default none |

### Alert Icon Variables

| Variable                  | Default Value | Description                          |
| ------------------------- | ------------- | ------------------------------------ |
| `--alert-icon-padding-x`  | `0rem`        | Vertical padding around the content  |
| `--alert-icon-padding-y`  | `0.25rem`     | Horizonal padding around the content |
| `--alert-icon-height`     | `24px`        | Default icon height                  |
| `--alert-icon-min-height` | `24px`        | Prevent shrinking of icon            |
| `--alert-icon-width`      | `24px`        | Default icon width                   |
| `--alert-icon-min-width`  | `24px`        | Prevent shrinking of icon            |

#### Success Alert Variables

| Variable                           | Default Value                | Description               |
| ---------------------------------- | ---------------------------- | ------------------------- |
| `--alert-text-success-color`       | `var(--color-success)`       | Text content colour       |
| `--alert-background-success-color` | `var(--color-success-light)` | Background content colour |

#### Info Alert Variables

| Variable                        | Default Value                  | Description               |
| ------------------------------- | ------------------------------ | ------------------------- |
| `--alert-text-info-color`       | `var(--color-attention)`       | Text content colour       |
| `--alert-background-info-color` | `var(--color-attention-light)` | Background content colour |

#### Error Alert Variables

| Variable                         | Default Value              | Description               |
| -------------------------------- | -------------------------- | ------------------------- |
| `--alert-text-error-color`       | `var(--color-error)`       | Text content colour       |
| `--alert-background-error-color` | `var(--color-error-light)` | Background content colour |

#### Accessibility

In these examples, the svg icon has the aria-hidden attribute so that the icon is not visible to the Accessibility tree. This is appropriate as these icons are not interactive and are considered decorative, as the paragraph content contains the information needing to be relayed to the screenreader.
