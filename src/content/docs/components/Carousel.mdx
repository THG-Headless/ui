---
title: Carousel
description: Carousel
---

import ShowComponent from '@components/doc-helpers/ShowComponent.astro';
import BasicCarousel from '@components/carousel/BasicCarousel.html';
import CarouselContent from '@components/carousel/CarouselContent.html';
import CarouselBanner from '@components/carousel/CarouselBanner.html';
import Bullets from '@components/carousel/CarouselBullets.html';
import CarouselControls from '@components/carousel/CarouselControls.html';
import Counter from '@components/carousel/CarouselCounter.html';
import CarouselBulletControlled from '@components/carousel/CarouselBulletControlled.html';
import CarouselButtonControlled from '@components/carousel/CarouselButtonControlled.html';
import CarouselBannerControlled from '@components/carousel/CarouselBannerControlled.html';

Used to display lists of cards in a horizontally scrollable area.

## Basic Carousel

<ShowComponent>
  <BasicCarousel />
</ShowComponent>

## Carousel Content

<ShowComponent>
  <CarouselContent />
</ShowComponent>

## Banner Carousel

<ShowComponent>
  <CarouselBanner />
</ShowComponent>

## Bullet Controls

<ShowComponent>
  <Bullets />
</ShowComponent>

## Button Controls

Use these to navigate between slides.

<ShowComponent>
  <CarouselControls />
</ShowComponent>

They use the`btn` class (see [Button](/components/button/)), and additional styling is applied by the `carousel-control-btn` modifier class.

## Counter

<ShowComponent>
  <Counter />
</ShowComponent>

## Examples

### Carousel with Bullet Controls

<ShowComponent>
  <CarouselBulletControlled />
</ShowComponent>

### Carousel with Button Controls

<ShowComponent>
  <CarouselButtonControlled />
</ShowComponent>

### Carousel Banner with Controls

<ShowComponent>
  <CarouselBannerControlled />
</ShowComponent>

## Variables

The carousel can be customized with the following variables:

## Accessibility

- [x] Each carousel wrapper has the `aria-roledescription` property set to 'carousel'.
- [x] Each slide container has role group with the property `aria-roledescription` set to slide.
- [x] You can to tab through the slides using `Tab` and `Shift+Tab`

In addition, you must implement the following:

- [ ] Make sure each slide has an accessible name

- see [this guide](https://www.w3.org/WAI/ARIA/apg/patterns/carousel/#wai-ariaroles,states,andproperties) for more information

## Interactivity

- [ ] When implementing interactivity for the buttons and pips, we recommended to use the [Intersection Observer API](https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API) to add interactivity.

## Further reading

Carousel Pattern on W3C Web Accessibility Initiative
https://www.w3.org/WAI/ARIA/apg/patterns/carousel/

W3.org carousel tutorial
https://www.w3.org/WAI/tutorials/carousels/

CSS Only Carousel CSS-Tricks
https://css-tricks.com/css-only-carousel/
