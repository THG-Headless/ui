---
title: Defining Your Theme
description: Specifying styles to your project
sidebar:
    order: 3
---
import ColorScheme from "../../../components/Colorscheme.astro"
import ShowComponent from "../../../components/ShowComponent.astro"

Default CSS variables are provided, which match the style shown in the documentation. These can be overwritten within your project by defining the specified css variables.

On this page, variables that effect many components are documented. Beyond this, additional css variables exist for specific components. Documentation of these values can be found on component specific pages.

## Colour Schemes
The library uses the colour schemes defined in the main css file.

<ShowComponent filePath="./src/tailwind.css">
    <ColorScheme/>
</ShowComponent>

{/* <ColorScheme/> */}